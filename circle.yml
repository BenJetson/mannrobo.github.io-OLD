machine:
  ruby:
    version: 2.3.0
test:
  override:
    - cd ~/mannrobo.github.io
    - ./.tests/config-check.sh
    - gem install github-pages
    - jekyll build
#general:
#  branches:
#    only:
#      - master
