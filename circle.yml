machine:
  ruby:
    version: 2.3.0
dependencies:
  override:
    - gem install github-pages
    - gem environment
    - ls /var/lib/gems/2.3.0/gems
  cache_directories:
    - "/var/lib/gems/2.3.0/gems"
test:
  override:
    - cd ~/mannrobo.github.io
    - ./.tests/config-check.sh
    - jekyll build
#general:
#  branches:
#    only:
#      - master
